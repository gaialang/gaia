package main

import "xyz"

/*
struct Book {
    title: string
    years: int[]
}

interface Student {
    read() -> void
    write(a: string[][]) -> string
}

enum AAA {
    X = 1,
    Y,
    Z,
}
*/

var abc: int = "xyz"
var abc: string

/*
func main() -> int {
    // a = b;
    printf("%d", f[1])
    return 0
}

// comments
func foo(i: int, j: int) -> int {
    return i + j / 2
}
*/
