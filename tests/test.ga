package main

struct Book {
    title: string
    years: int[]
}

interface Su {
    read() -> void
    write(a: string[][]) -> string
}

enum AAA {
    X = 1,
    Y,
    Z,
}

var abc: int = 1

func main() -> int {
    abc = 4;
    foo(2, 3);
    return 0
}

// comments
func foo(i: int, j: int) -> int {
    return i + j / 2
}
