package main

struct Book {
    title: string
    years: int[]
}

interface Student {
    read() -> void
    write(a: string[][]) -> string
}

enum AAA {
    X = 1,
    Y,
    Z,
}

var a: int = 0

func main() -> int {
    var f: int[] = [1,2,]
    f[1] = 3
    printf("%d", f[1])
    return 0
}

/* comments */
// comments
func foo(i: int, j: int) -> int {
    return i + j / 2
}
